import CollectionPage from "@/components/CollectionPage";

const Subscriptions = () => {
    return (
        <CollectionPage
            type="subscription"
            title="Subscriptions"
            description="Monitor your recurring service payments and cycles."
        />
    );
};

export default Subscriptions;
