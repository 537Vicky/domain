import CollectionPage from "@/components/CollectionPage";

const Domains = () => {
    return (
        <CollectionPage
            type="domain"
            title="Domains"
            description="Keep track of your domain names and registration expiry."
        />
    );
};

export default Domains;
